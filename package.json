{
  "name": "next_ecommerce",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run frontend & pnpm run backend",
    "frontend": "next dev && echo 'Frontend server started successfully' || echo 'Frontend server failed to start'",
    "backend": "php artisan serve && echo 'Backend server started successfully' || echo 'Backend server failed to start'",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "pnpm exec jest"
  },
  "dependencies": {
    "next": "13.4.19",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
